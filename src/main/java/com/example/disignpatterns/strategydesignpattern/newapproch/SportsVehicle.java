package com.example.disignpatterns.strategydesignpattern.newapproch;

import com.example.disignpatterns.strategydesignpattern.newapproch.run.Run;

public class SportsVehicle extends Vehicle {

    public SportsVehicle(Run run) {
        super(run);
    }
}
